<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">

    <record id="excep_order_wrong_total_amount" model="exception.rule">
        <field name="name">Total Amount differs from Woocommerce</field>
        <field name="description">The amount computed in Odoo doesn't match with the amount in Woocommerce.

Cause:
The taxes are probably different between Odoo and Woocommerce. A fiscal position could have changed the final price.

Resolution:
Check your taxes and fiscal positions configuration and correct them if necessary.</field>
        <field name="sequence">30</field>
        <field name="rule_group">sale</field>
        <field name="model">sale.order</field>
        <field name="code">if sale.woo_bind_ids and abs(sale.amount_total - sale.woo_bind_ids[0].total_amount) >= 0.001:
    failed = True</field>
        <field name="active" eval="True"/>
    </record>
</odoo>
